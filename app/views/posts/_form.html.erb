
  
      <%= form_with(model: post,html:{data:{action: "turbo:submit-end->turbo-modal#close"}}) do |form|   %>
          <% if post.errors.any? %>
            <div style="color: red">
              <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

              <ul>
                <% post.errors.each do |error| %>
                  <li><%= error.full_message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="form">
            <div >
              <%= form.label :name, style: "display: block" %>
              <%= form.text_field :name,  required: true %>
            </div>

            <div  >
              <%= form.label :description, style: "display: block" %>
              <%= form.text_field :description, required: true%>
            </div>
          </div>  
          <div class="mb-6 mt-4">
            
              <div class="dropzone dropzone-default" data-controller="dropzone">
                <%= form.file_field :avatar, direct_upload: true, disabled: true, readonly: 'readonly', multiple: true, data: { target: 'dropzone.input' } %>
                <div class="dropzone-msg dz-message needsclick">
                  <h3 class="dropzone-msg-title text-muted fs-6">Drop <span class="fw-bold">file(s)</span> here or</h3>
                  <button type="button"  class='btn btn-light border-primary text-primary mt-2'>Select File</button>
                </div>
              </div>

              
                <div data-controller="apna">
                
                </div>
            

          <div class=" mt-4"   >
            <%= form.submit  class:"btn btn-primary" %>
          </div>
          
          

      <% end %>
  </div>    
 
